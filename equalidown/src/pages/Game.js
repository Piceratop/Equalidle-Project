import EquationGrid from "../components/EquationGrid";
import Numpad from "../components/Numpad";
const Game = () => {
  return (
    <main>
      <EquationGrid />
      <Numpad />
    </main>
  );
};

export default Game;
